% Standalone TikZ render template — the user-owned template the tikz renderer
% wraps a tikz file in (the QTikz .pgs model: the body marker below is replaced
% with the tikz file source, then compiled). The template OWNS its preamble via
% \usepackage{dzg-tikz} (the user's centralized styles tree); the app injects
% nothing — edit this template and your styles tree to control how every figure
% compiles.
\documentclass[tikz,border=2pt]{standalone}
\usepackage{dzg-tikz}
\usepackage{graphicx}
\usepackage{import}

% Additional TikZ libraries beyond what dzg-preamble loads
\usetikzlibrary{external}

\begin{document}
\nopagecolor
$body$
\end{document}
