\begin{tikzpicture}[
  scale=0.8,
  latticept/.style={circle, fill=black!50, inner sep=0pt, minimum size=4.2pt},
  blatticept/.style={fill=black},
  openpt/.style={circle, draw=black, fill=white, line width=1.1pt,
    inner sep=0pt, minimum size=8pt},
  edge/.style={line width=2pt, line cap=round},
  lightedge/.style={line width=2pt, color=gray!50, line cap=round},
  gridline/.style={gray!45, dashed, line width=0.35pt},
  region/.style={fill=cyan!8, draw=none},
  every node/.style={font=\small}
]
  % grid
  \foreach \x in {-2,-1,...,7} {
    \draw[gridline] (\x,-2.5) -- (\x,4.5);
  }
  \foreach \y in {-2,-1,...,4} {
    \draw[gridline] (-2.5,\y) -- (7.5,\y);
  }

  % lattice points
  \foreach \x in {-2,-1,...,7} {
    \foreach \y in {-2,-1,...,4} {
      \node[latticept] at (\x,\y) {};
    }
  }

  % vertices
  \coordinate (ul) at (0,4);
  \coordinate (ll) at (0,0);
  \coordinate (r)  at (5,0);

  % shaded region
  \fill[region] (ul) -- (r) -- (ll) -- cycle;

  % boundary
  \draw[lightedge, line width=0.8pt] (ul) -- (ll);
  \draw[lightedge, line width=0.8pt] (ll) -- (r);
  \draw[edge, line width=0.8pt] (ul) -- (r);

  % endpoints
  \node[openpt] at (ul) {};
  \node[openpt] at (ll) {};
  \node[openpt] at (r)  {};

  % distinguished point
  \node[
    star,
    star points=5,
    star point ratio=2.2,
    draw=red,
    fill=white,
    line width=1.2pt,
    minimum size=12pt,
    inner sep=0pt
  ] at (2.5,2) {};

  % labels
  \node[red, font=\Large] at (2.5,2.5) {$p_\ast$};

  \node[font=\Large] at (1.5,1.5) {$\widetilde E_8$};

  % redraw lattice points on top of the region
  \foreach \y in {1,2,3} {
    \node[latticept, blatticept] at (0,\y) {};
  }

  \node[latticept, blatticept] at (1,1) {};
  \node[latticept, blatticept] at (1,2) {};
  \node[latticept, blatticept] at (1,3) {};

  \node[latticept, blatticept] at (2,1) {};
  \node[latticept, blatticept] at (2,2) {};

  \node[latticept, blatticept] at (3,1) {};


  % points on the hypotenuse
  \node[latticept, blatticept] at (1,3) {};
  \node[latticept, blatticept] at (2,2) {};
  \node[latticept, blatticept] at (3,1) {};
  \node[latticept, blatticept] at (4,0) {};
\node[latticept, blatticept] at (1,0) {};
\node[latticept, blatticept] at (2,0) {};
\node[latticept, blatticept] at (3,0) {};
\end{tikzpicture}