\documentclass[tikz,border=2pt]{standalone}
\usepackage{dzg-tikz}

\begin{document}
% Coend universal property commutative triangle
% F(c,c) (apex) --ω_c--> coend ∫^{c∈C}F(c,c) (bottom-left)
% F(c,c) --ζ_c--> X (bottom-right)
% coend --∃!h--> X (dashed base arrow)
% Commutativity: h ∘ ω_c = ζ_c
\begin{tikzcd}
    F(c,c)
        \arrow[d, swap, "\omega_c"]
        \arrow[dr, "\zeta_c"] \\[1.5em]
    \displaystyle\int^{c\in C} F(c,c)
        \arrow[r, dashed, "\exists!\,h"]
    & X
\end{tikzcd}
\end{document}
