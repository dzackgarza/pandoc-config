\documentclass[tikz,border=2pt]{standalone}
\usepackage{dzg-tikz}

\begin{document}
% Cowedge/Coend dinaturality commutative square
% Canonical: F(B,A) -> F(A,A) via F(f,A) (top), F(B,A) -> F(B,B) via F(B,f) (left)
%            h_A: F(A,A) -> X (right), h_B: F(B,B) -> X (bottom)
% Commutativity: h_A ∘ F(f,A) = h_B ∘ F(B,f)
\begin{tikzcd}
F(B, A) \arrow[r, "{F(f, A)}"] \arrow[d, "{F(B, f)}"']
  & F(A, A) \arrow[d, "{h_A}"] \\
F(B, B) \arrow[r, "{h_B}"']
  & X
\end{tikzcd}
\end{document}
