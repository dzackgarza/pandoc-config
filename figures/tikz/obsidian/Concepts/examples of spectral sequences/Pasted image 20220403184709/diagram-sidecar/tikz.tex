% Serre spectral sequence of the path-loop fibration ΩS⁴ → PS⁴ → S⁴:
% homological E² = E⁴ page with d⁴ differentials of bidegree (−4, +3).
% Only columns p = 0 and p = 4 are populated (H_*(S⁴) ≅ ℤ in degrees 0, 4).
% For q = 3, 4, 5 the target of d⁴ lies above the page.
\documentclass[tikz,border=2pt]{standalone}
\usepackage{dzg-tikz}

\begin{document}
\begin{tikzpicture}
  \pic (E) [ss homological, ss column sep=1.6cm] {ss page};
  \foreach \q in {0,...,5} \foreach \p in {0,4}
    {\ssentry{E}{\p}{\q}{$H_{\q}(\Omega S^{4})$}}
  \begin{scope}
    \clip (E--1--1) rectangle (E-6-6);
    \foreach \q in {0,...,5} {\ssdifferential[ss unlabelled]{E}{4}{4}{\q}}
  \end{scope}
\end{tikzpicture}
\end{document}
