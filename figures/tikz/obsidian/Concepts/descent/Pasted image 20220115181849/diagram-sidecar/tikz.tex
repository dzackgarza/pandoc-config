% Shared Tikz Preamble for Diagram Atomization Pipeline
% Load this in every tikz generated by Phase 5 subagents.
% Do NOT redefine these — use exactly this preamble.

\documentclass[tikz,border=2pt]{standalone}

% Core libraries
\usetikzlibrary{
    arrows.meta,       % arrow styles
    positioning,       % relative placement
    cd,                % commutative diagrams (tikz-cd)
    calc,              % coordinate calculations
    shapes.geometric,  % ellipses, polygons, etc.
    shapes.misc,       % rounded rectangles, etc.
    decorations.markings,  % arrow on paths
    backgrounds,       % background layer
    fit,               % fitting nodes around others
}

% Standard arrow tip
\tikzset{>=Stealth}

% Commutative diagram style
\tikzcdset{
    arrows={>=Stealth},
    row sep/normal=3em,
    column sep/normal=4em,
}

% Common node styles
\tikzset{
    mathnode/.style={rectangle, draw=none, fill=none, inner sep=2pt},
    labelnode/.style={rectangle, draw=none, fill=none, font=\small, inner sep=1pt},
}

\begin{document}
% Descent Cocycle Condition Commutative Triangle (Diagram 15.2)
%
% Vertices:
%   B ⊗_A B ⊗_A N  (top-left, id: BBN)
%   B ⊗_A N ⊗_A B  (top-right, id: BNB)
%   N ⊗_A B ⊗_A B  (bottom, id: NBB)
%
% Edges:
%   φ_{23} = id_B ⊗ φ   : BBN → BNB  (top edge)
%   φ_{12} = φ ⊗ id_B   : BNB → NBB  (right slanted edge)
%   φ_{13} = φ_{12} ∘ φ_{23} : BBN → NBB  (left slanted edge)
%
% Cocycle condition: φ_{13} = φ_{12} ∘ φ_{23}
%
\begin{tikzcd}
B \otimes_A B \otimes_A N
    \arrow[r, "\phi_{23}"]
    \arrow[dr, "\phi_{13}"']
&
B \otimes_A N \otimes_A B
    \arrow[d, "\phi_{12}"]
\\
&
N \otimes_A B \otimes_A B
\end{tikzcd}
\end{document}
