\documentclass[11pt]{article}

\usepackage{mathpazo}           % Palatino text and math
\usepackage{dzg-tikz}
\usepackage[inline]{asymptote}

\begin{document}

% Coffee cup with a map out (same drawing as MapOutOfCoffeeCup.tikz, twice the scale)
\begin{tikzpicture}[scale=\textwidth/5cm]
  % handle
  \begin{scope}[shift=(10:7/8), rotate=-30, yslant=1/2, xslant=-1/8]
    \shade[top color=dzg muted, bottom color=dzg grid]
      (0,0) arc (130:-100:3/8 and 1/2) -- ++(0,1/4) arc (-100:130:1/8 and 1/4) -- cycle;
    \shade[top color=dzg blob, bottom color=dzg muted]
      (0,0) arc (130:-100:3/8 and 1/2) -- ++(0,1/32) arc (-100:130:1/4 and 1/3) -- cycle;
  \end{scope}

  % cup
  \fill[dzg muted, path fading=fade out] (0,-1) ellipse [x radius=3/4, y radius=1/2];
  \shade[left color=dzg muted, right color=dzg grid] (-1,0) arc (180:360:1 and 5/4);
  \shade[bottom color=dzg muted, top color=dzg grid, opacity=1/2]
    (-1,0) arc (180:360:1 and 5/4);
  \shade[left color=dzg blob, right color=dzg grid]
    (0,0) ellipse [x radius=1, y radius=1/2];
  \shade[left color=dzg grid, right color=dzg blob]
    (0,0) ellipse [x radius=1-1/16, y radius=1/2-1/16];
  \shade[bottom color=dzg muted, top color=dzg blob, opacity=1/2]
    (0,0) ellipse [x radius=1-1/16, y radius=1/2-1/16];
  \node at (0,-1.7) {$X = $ A Coffee Cup};

  \draw[map arrow] (1.5,-.5) to[curve through={(2.04,-0.5) (2.86,-0.17)}] (4.2,-.5);
  \node at (3,0) {$f: X \to Y$};
\end{tikzpicture}

% Torus in R^3 (Asymptote; produces no output)
\begin{asy}
  size(400);
  import graph3;

  currentprojection=perspective(5,4,4);
  real R=3;
  real a=1;

  triple fs(pair t) {
    return ((R+a*Cos(t.y))*Cos(t.x),(R+a*Cos(t.y))*Sin(t.x),a*Sin(t.y));
  };

  surface s=surface(fs,(0,180),(360,360),8,8,Spline);
  draw(s,surfacepen=material(blue+opacity(0.6), emissivepen=0.2*white),render(compression=Low,merge=true));

  xaxis3(Label("$x$",1),xmin=0,xmax=7,Arrow3);
  yaxis3(Label("$y$",1),ymin=0,ymax=7,Arrow3);
  zaxis3(Label("$z$",1),zmin=0,zmax=4,Arrow3);
  label("$Y = $ A Torus", (0,-40), p = fontsize(30pt));
\end{asy}

% Spheres S^0, S^1, S^2
\begin{tikzpicture}[scale=\textwidth/35cm]
  \node[point, dzg singular] at (-7,0) {};
  \node[point, dzg singular] at (-6,0) {};
  \node at (-6.5,-1.3) {$S^0$};

  \draw[dzg singular] (-3,0) circle (1cm);
  \node at (-3,-1.3) {$S^1$};

  \draw[dzg singular] (-1,0) arc (180:360:1cm and 0.5cm);
  \draw[dzg singular, dashed] (-1,0) arc (180:0:1cm and 0.5cm);
  \draw[dzg singular] (0,1) arc (90:270:0.5cm and 1cm);
  \draw[dzg singular, dashed] (0,1) arc (90:-90:0.5cm and 1cm);
  \draw[dzg singular] (0,0) circle (1cm);
  \shade[ball color=dzg region, opacity=0.2] (0,0) circle (1cm);
  \node at (0,-1.3) {$S^2$};
\end{tikzpicture}

% Balls B^0, B^1, B^2, each with its boundary sphere
\begin{tikzpicture}[scale=\textwidth/35cm]
  \draw[distinguished curve] (-7,0) -- (-6,0);
  \node[point, dzg singular] at (-7,0) {};
  \node[point, dzg singular] at (-6,0) {};
  \node at (-6.5,-1.3) {$B^0$};

  \draw[pattern=horizontal lines, pattern color=dzg accent] (-3,0) circle (1cm);
  \draw[dzg singular] (-3,0) circle (1cm);
  \node at (-3,-1.3) {$B^1$};

  \shade[ball color=dzg accent, opacity=0.9] (0,0) circle (1cm);
  \draw[dzg singular] (0,0) circle (1cm);
  \draw[dzg singular] (-1,0) arc (180:360:1cm and 0.5cm);
  \draw[dzg singular] (0,1) arc (90:270:0.5cm and 1cm);
  \node at (0,-1.3) {$B^2$};
\end{tikzpicture}

\end{document}
