% Stratification poset of \overline{M}_2 (objects/moduli/m2-strata.json, laid
% out by \posetfromjson) as Div(24) \ {1}: each stratum is labelled by its
% divisor, with its stable graph beside it (outside the poset) and its
% dimension, the grade, at the left. The header states the isomorphisms with
% Div(24) and with the subdiagrams of the Coxeter diagram
% objects/moduli/m2-coxeter.
\begin{tikzpicture}[font=\small]
  \posetfromjson[name prefix=P-, grade distance=2cm, element width=9mm,
    element height=6mm, element sep=3.6cm, element style=poset node,
    cover style=degeneration]{objects/moduli/m2-strata.json}
  \path (P-24); \pgfgetlastxy{\topx}{\topy}
  \foreach \e in \posetelements {
    \path (P-\e); \pgfgetlastxy{\ex}{\ey}
    \ifdim\ex<\topx
      \pic at ([xshift=-1.6cm]P-\e.west) {object=moduli/m2-graph-\e};
    \else
      \pic at ([xshift=1.6cm]P-\e.east) {object=moduli/m2-graph-\e};
    \fi
  }
  \coordinate (left) at ([xshift=-5mm]current bounding box.west);
  \foreach \dim in {0,...,3}
    \node[anchor=east] at (left |- 0,{(\dim-3)*2cm}) {dim $\dim$};
  \node[font=\Large, above=0.6cm of P-24] {$\mathrm{Strata}(\overline{\mcm}_{2, 0})
    \cong \mathrm{Div}(24)\sm\ts{1}
    \cong \mathrm{SubDiag}\qty{\vcenter{\hbox{\tikz[x=1em, y=1em]{\pic {object=moduli/m2-coxeter};}}}
      \subseteq \I_{1, 3}(2)\tensor_\ZZ \RR} - \ts{\emptyset}$};
\end{tikzpicture}
