% Stratification poset of \overline{M}_2 (dzg-moduli.tex) as Div(24) \ {1}:
% each stratum is labelled by its divisor, with its stable graph beside it and
% its dimension at the left. The header states the isomorphisms with Div(24)
% and with the subdiagrams of the Coxeter diagram `m2 coxeter`.
\begin{tikzpicture}[font=\small]
  \pic[m2 render=divisor, m2 layout=vertical] (P) {m2 strata};
  \foreach \d/\dim/\n/\row/\k/\c in \dzgMtwoStrata {
    \ifnum\row>0
      \pic (G\d) at ([xshift=-3.2cm]P-\d-box) {m2 graph=\d};
    \else
      \pic (G\d) at ([xshift=2.6cm]P-\d-box) {m2 graph=\d};
    \fi
    \node[anchor=east] at (-7.4,-2*\n) {dim $\dim$};
  }
  \node[font=\Large, above=0.6cm of P-24-box] {$\mathrm{Strata}(\overline{\mcm}_{2, 0})
    \cong \mathrm{Div}(24)\sm\ts{1}
    \cong \mathrm{SubDiag}\qty{\vcenter{\hbox{\tikz[x=1em, y=1em]{\pic {m2 coxeter};}}}
      \subseteq \I_{1, 3}(2)\tensor_\ZZ \RR} - \ts{\emptyset}$};
\end{tikzpicture}
